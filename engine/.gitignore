# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Service worker gerado pelo vite-plugin-pwa quando devOptions está ligado.
dev-dist
*.local

# Referências visuais do Jesse (moodboard): arte de terceiros, nunca asset do
# produto. Fica só na máquina local.
ui designs/

# Amostra da FIPE baixada para medir o parser de versão. Não é versionada de
# propósito: medir contra uma cópia velha é medir o passado. Rode
# `node scripts/fetch-fipe-sample.mjs` para regerar.
scripts/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
